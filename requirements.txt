# NormalizedDynamics Requirements
# ================================
#
# This repository uses a fully pinned lock file for reproducible installs:
# - requirements.lock.txt
#
# Install with:
#   pip install -r requirements.lock.txt
#
# Keep this file as a pointer so "requirements.txt" always means "exact env".

-r requirements.lock.txt